import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <section
      class="min-h-[80vh] flex items-center bg-[url('/images/mobile2.png')] bg-cover bg-no-repeat md:bg-[url('/images/logoforbanner.png')]"
    >
    </section>
  );
});